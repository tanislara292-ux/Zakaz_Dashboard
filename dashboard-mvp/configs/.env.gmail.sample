# Gmail API Configuration (резервный канал)
# Скопируйте этот файл в secrets/.env.gmail и заполните реальные значения

# Путь к файлу credentials.json (OAuth2)
GMAIL_CREDENTIALS_PATH=secrets/gmail/credentials.json

# Путь к файлу token.json (авторизованный токен)
GMAIL_TOKEN_PATH=secrets/gmail/token.json

# Email адрес для фильтрации писем
GMAIL_EMAIL_ADDRESS=your_email@gmail.com

# Фильтр для поиска писем (по умолчанию ищет письма с темами QTickets)
GMAIL_QUERY=subject:QTickets OR subject:qTickets OR from:qtickets.ru

# Количество дней для поиска писем (по умолчанию 7)
GMAIL_DAYS_BACK=7

# Таймаут запросов в секундах
GMAIL_TIMEOUT=30

# Настройки логирования
LOG_LEVEL=INFO
LOG_JSON=false
LOG_FILE=logs/gmail.log