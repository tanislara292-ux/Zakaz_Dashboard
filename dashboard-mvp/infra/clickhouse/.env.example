# ClickHouse configuration variables
# These variables are used by bootstrap scripts ONLY, NOT passed to container
# WARNING: Do NOT set CLICKHOUSE_USER, CLICKHOUSE_PASSWORD, CLICKHOUSE_DB
# as environment variables anywhere (docker-compose.yml or .env file) as this
# causes ClickHouse to create a default-user.xml file that conflicts with users.d/00-admin.xml.
# Admin user is created exclusively through users.d/00-admin.xml.
CLICKHOUSE_ADMIN_USER=admin
CLICKHOUSE_ADMIN_PASSWORD=admin_pass
CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
CLICKHOUSE_TZ=Europe/Moscow

# Container port mappings
CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_NATIVE_PORT=9000
